# licenseadder
A script for adding license header and license files to a source of a project

Example usage:

# ./licenseadder.py --project $PROJECT_NAME --dir $PROJECT_DIR --license lgpl3  --author "Johan Henriksson johan[a]dexar.se" --name "example"



